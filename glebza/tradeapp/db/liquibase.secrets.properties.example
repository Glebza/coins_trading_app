# Copy to liquibase.secrets.properties (gitignored). Do not commit real passwords.
# Used by bootstrap (trader.password, superuser liquibase.command.password) and optionally app migrations.

# Superuser password when running bootstrap with liquibase.bootstrap.properties
liquibase.command.password=

# Password for CREATE ROLE trader (bootstrap changelog only)
trader.password=CHANGE_ME
